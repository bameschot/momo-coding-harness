ollama>=0.5,<1
# Code navigation (code_outline / find_symbol / read_symbol / find_references /
# file_dependencies).
# Per-language grammar wheels are bundled and work offline; the harness still
# runs without them, the code-navigation tools are just not offered.
tree-sitter>=0.25,<0.27
tree-sitter-python>=0.25
tree-sitter-java>=0.23
tree-sitter-c>=0.24
tree-sitter-cpp>=0.23
tree-sitter-kotlin>=1.1
tree-sitter-rust>=0.24
tree-sitter-javascript>=0.25
tree-sitter-typescript>=0.23
# PDF attachments in the web UI (converted to plain text for the model). Pure
# Python, no dependencies; without it text-file attachments still work.
pypdf>=6,<7
